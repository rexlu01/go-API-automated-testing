syntax = "proto3";

package go.micro.srv.showdata;

service ShowData {
	rpc GetDataCrontab(TryRequest) returns (TryRespons) {}
}

message TryRequest {
	string flag = 1;
}

message TryRespons {
	repeated double Mem = 1;
	repeated double CPU = 2;
}